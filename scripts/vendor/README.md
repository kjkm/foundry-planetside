# Vendored dependencies

`three.module.js` is copied here by `npm run vendor` (see project root `setup.js`).

Do not edit files in this directory by hand. Run `npm install` to install dependencies and `npm run vendor` to populate this folder.
